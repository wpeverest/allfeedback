# Required for pnpm to work correctly with @wordpress/scripts
# Some WP script dependencies are not hoisted by default in pnpm
shamefully-hoist=true

# Strict peer dependency checks (relax for WP ecosystem)
strict-peer-dependencies=false
